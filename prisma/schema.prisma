datasource db {
  url      = env("CONFG_DATABASE_URL")
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

enum ConfigValueType {
  STRING
}

model Config {
  id        Int      @id @default(autoincrement())
  name      String
  type      ConfigValueType
  value     String?
}
